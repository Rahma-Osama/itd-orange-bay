enum NetworkTimeoutExceptions {
  sendTimeout,
  connectionTimeout,
  receiveTimeout,
  cancel,
}

enum NetworkExpMsgCodes {
  noInternetConnectionExpCode,
  badResponseExpCode,
  unKnowingExpCode,
}
